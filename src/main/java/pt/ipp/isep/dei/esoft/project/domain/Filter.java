/*
package pt.ipp.isep.dei.esoft.project.domain;

public class Filter {

    private String name;
    private int minPrice;
    private int maxPrice;

    public Filter() {
        this.name = "";
        this.minPrice = Integer.MIN_VALUE;
        this.maxPrice = Integer.MAX_VALUE;
    }

    public void defineCriteria(String name, int minPrice, int maxPrice) {
        this.name = name;
        this.minPrice = minPrice;
        this.maxPrice = maxPrice;
    }

   /* public boolean matches(Property property) {
        if (!property.getName().contains(name)) {
            return false;
        }
        if (property.getPrice() < minPrice || property.getPrice() > maxPrice) {
            return false;
        }
        return true;
    }

}
*/
